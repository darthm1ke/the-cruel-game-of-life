{
  "name": "the-cruel-game-of-life",
  "version": "0.1.0",
  "description": "A brutal vicious-cycle survival clicker. The body isn't the joke — the system is.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
